StylesPath = styles
MinAlertLevel = suggestion
Vocab = Portfolio

# Microsoft 和 Google 规则快照已经提交到 styles/，确保本地与 CI 使用同一份规则。
# 更新上游规则时，应单独执行并审查变更，再提交新的规则快照。
[*.{md,txt}]
BasedOnStyles = Vale, Microsoft, Google, MyStyle
