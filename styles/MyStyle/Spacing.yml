extends: existence
message: "错误：中英文之间必须加空格"
level: error
# scope: raw
# 关键修改：
# 1. 使用双引号 "..." 以支持 \u 转义
# 2. 前后加上 .* ，意思是：不管这个词多长，只要它"里面包含"中英紧挨的情况，就抓出来。
tokens:
  - ".*[\u4e00-\u9fa5][a-zA-Z0-9].*"  # 抓取：(任意字)中文+英文(任意字)
  - ".*[a-zA-Z0-9][\u4e00-\u9fa5].*"  # 抓取：(任意字)英文+中文(任意字)